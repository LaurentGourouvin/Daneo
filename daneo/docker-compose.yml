services:
  postgres:
    image: postgres:17-alpine
    container_name: daneo-postgres

    ports:
      - "5432:5432"

    environment:
      POSTGRES_DB: daneo
      POSTGRES_USER: daneo
      POSTGRES_PASSWORD: daneo

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: